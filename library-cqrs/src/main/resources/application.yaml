spring:
    application:
        name: library-cqrs
    datasource:
        url: jdbc:postgresql://localhost:5433/libraryproject
        username: admin
        password: admin
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
