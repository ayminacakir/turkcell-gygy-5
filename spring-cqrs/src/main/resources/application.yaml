spring:
    application:
        name: spring-cqrs
    datasource:
        url: jdbc:postgresql://localhost:5433/eticaret
        username: admin
        password: admin
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
